---
interface Props {
    title: string;
    href?: string;
    center?: true;
    color?: string;
}

const { title, href, center, color } = Astro.props;
---

<div
    class="m-2 rounded-md hover:bg-[#ffffff05] px-4 py-4 hover:shadow-md hover:ring-1 hover:ring-inset hover:ring-[#ffffff08] transition-colors duration-300 ease-in-out hover:bg-[#ffffff10]"
>
    <div class="box-border">
        <h3
            class={`text-2xl font-bold text-neutral-300 ${
                center ? "ml-auto mr-auto text-center" : ""
            }`}
        >
            {
                href ? (
                    <a
                        href={href}
                        class="transition-colors duration-300 ease-in-out hover:text-sky-400 hover:underline"
                        style={{ color: color ? color : "text-sky-600" }}
                    >
                        {title}
                    </a>
                ) : (
                    title
                )
            }
        </h3>
        <p class="text-neutral-300">
            <slot />
        </p>
    </div>
</div>
